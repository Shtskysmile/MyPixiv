# 更新头像 404 问题修复说明

## 问题描述
用户在个人主页点击"编辑信息"并更新头像时，遇到 404 错误：

```
POST http://localhost:8080/user/updateUserInfo 404 (Not Found)
```

**错误信息**：
```json
{
    "message": "Request failed with status code 404",
    "name": "AxiosError",
    "code": "ERR_BAD_REQUEST",
    "status": 404
}
```

## 问题原因

### 根本原因
前端调用的 API `/user/updateUserInfo` 没有在 `vue.config.js` 中配置代理。

### 为什么会发生这个问题？
1. **之前的修复**：为了解决直接访问 `/user/:id` 路由时报 500 错误的问题，我们从代理配置中移除了 `user` 路径
2. **副作用**：移除 `user` 后，所有以 `/user/` 开头的 **API 请求**也无法被代理到后端了
3. **现象**：
   - ✅ 前端路由 `/user/:id` 正常工作（由 Vue Router 处理）
   - ❌ API 请求 `/user/updateUserInfo` 返回 404（没有代理到后端）

### 混淆的概念
| 类型 | 路径示例 | 处理方式 |
|------|---------|----------|
| **前端路由** | `/user/6c2762d4-b792-11f0-8af4-00ff77e93bfa` | Vue Router 处理，不应代理 |
| **后端 API** | `/user/updateUserInfo` | 应该代理到后端 8081 端口 |

之前的修复只考虑了前端路由，忽略了后端 API。

## 解决方案

### 修改 `vue.config.js`
在代理配置中添加一个新规则，**精确匹配**特定的 `/user/` API 路径。

**文件**：`mypixiv-vue/vue.config.js`

**修改位置**：第 65-81 行（新增）

```javascript
// 代理 /user/ 开头的 API 请求（如 updateUserInfo, concernUser 等）
// 注意：不包括 /user/:id 这样的路由路径
'^/user/(updateUserInfo|concernUser|unconcernUser)': {
  target: process.env.VUE_APP_API_BASE_URL || 'http://localhost:8081',
  changeOrigin: true,
  logLevel: 'debug',
  onProxyReq: (proxyReq, req, res) => {
    console.log(`🔄 代理请求: ${req.method} ${req.url} -> ${proxyReq.path}`)
  },
  onProxyRes: (proxyRes, req, res) => {
    console.log(`✅ 代理响应: ${proxyRes.statusCode} ${req.url}`)
  },
  onError: (err, req, res) => {
    console.error('❌ 代理错误:', err.message)
    console.error('💡 提示: 请确保后端服务已启动')
  }
},
```

### 为什么这样修改？
1. **精确匹配**：只代理 `updateUserInfo`、`concernUser`、`unconcernUser` 这三个 API
2. **不影响路由**：`/user/:id` 这样的路径不会被匹配，仍由 Vue Router 处理
3. **易于维护**：如果将来添加新的 `/user/` API，只需在正则表达式中添加即可

### 代理规则说明

**正则表达式**：`^/user/(updateUserInfo|concernUser|unconcernUser)`

- `^` - 匹配字符串开头
- `/user/` - 固定前缀
- `(updateUserInfo|concernUser|unconcernUser)` - 匹配这三个 API 名称之一

**匹配的路径**：
- ✅ `/user/updateUserInfo` - 代理到后端
- ✅ `/user/concernUser` - 代理到后端
- ✅ `/user/unconcernUser` - 代理到后端

**不匹配的路径**：
- ❌ `/user/6c2762d4-b792-11f0-8af4-00ff77e93bfa` - 前端路由，不代理
- ❌ `/user` - 前端路由，不代理

## 相关后端接口

### 1. 更新用户信息
**接口**：`POST /user/updateUserInfo`  
**控制器**：`UserController.java` 第 160-173 行

```java
@PostMapping("/user/updateUserInfo")
public Result<String> updateUserInfo(
        @RequestHeader("Authorization") String authHeader,
        @RequestParam ("newUsername") String newUsername,
        @RequestParam("newGender") Integer newGender,
        @RequestPart(name = "newAvatar", required = false) MultipartFile newAvatar) throws Exception {
    String userId = (String) TokenProcess.getAttributeFromToken(authHeader, "userId");
    boolean ok = userService.updateUserInfo(userId, newUsername, newGender, newAvatar);
    if (ok) {
        return Result.success("更新成功");
    }
    return Result.error("更新失败");
}
```

### 2. 关注用户
**接口**：`POST /user/concernUser`  
**控制器**：`UserController.java` 第 175-185 行

```java
@PostMapping("/user/concernUser")
public Result<String> concernUser(
        @RequestHeader("Authorization") String authHeader,
        @RequestParam ("concernedUserId") String concernedUserId) throws Exception {
    String userId = (String) TokenProcess.getAttributeFromToken(authHeader, "userId");
    boolean ok = userService.concernUser(userId, concernedUserId);
    if (ok) {
        return Result.success("关注成功");
    }
    return Result.error("关注失败");
}
```

### 3. 取消关注用户
**接口**：`POST /user/unconcernUser`  
**控制器**：`UserController.java`

```java
@PostMapping("/user/unconcernUser")
public Result<String> unconcernUser(
        @RequestHeader("Authorization") String authHeader,
        @RequestParam ("concernedUserId") String concernedUserId) throws Exception {
    String userId = (String) TokenProcess.getAttributeFromToken(authHeader, "userId");
    boolean ok = userService.unconcernUser(userId, concernedUserId);
    if (ok) {
        return Result.success("取消关注成功");
    }
    return Result.error("取消关注失败");
}
```

## 使用步骤

### 1️⃣ 重启前端服务（必须）
由于修改了 `vue.config.js`，必须重启前端服务：

```bash
cd mypixiv-vue
# Ctrl+C 停止当前服务
npm run serve
```

### 2️⃣ 测试更新头像功能

1. **登录并访问个人主页**  
   访问：`http://localhost:8080/user/你的用户ID`

2. **点击"编辑"按钮**  
   打开编辑个人信息的弹窗

3. **选择新头像并提交**  
   - 选择一个图片文件
   - 点击"保存"按钮

4. **验证结果**  
   - ✅ 不应该出现 404 错误
   - ✅ 应该显示"更新成功"提示
   - ✅ 页面刷新后显示新头像

### 3️⃣ 检查浏览器控制台

**成功的日志示例**：
```
🔄 代理请求: POST /user/updateUserInfo -> /user/updateUserInfo
✅ 代理响应: 200 /user/updateUserInfo
```

**如果失败**：
```
❌ 代理错误: connect ECONNREFUSED 127.0.0.1:8081
💡 提示: 请确保后端服务已启动
```

## 完整的代理配置结构

修复后的 `vue.config.js` 包含以下代理规则：

```javascript
proxy: {
  // 1. /api 开头的请求
  '/api': { ... },
  
  // 2. 用户认证相关的请求
  '^/(login|register|mySecurityIssues|verifySecurityIssue|updatePassword)': { ... },
  
  // 3. 内容相关的请求
  '^/(illustrations|mangas|contribution|search|userInfo|contributionList|favouriteList|likedList|concernedList|userCommentList)': { ... },
  
  // 4. /user/ 开头的 API 请求（新增）
  '^/user/(updateUserInfo|concernUser|unconcernUser)': { ... },
  
  // 5. 静态文件
  '/files': { ... }
}
```

## 故障排查

### Q1: 重启后仍然报 404 错误？
**解决方法**：
1. 确认前端服务已完全重启
2. 清除浏览器缓存
3. 检查后端服务是否正在运行
4. 查看浏览器控制台的代理日志

### Q2: 前端路由 `/user/:id` 不能访问了？
**解决方法**：
- 这不应该发生。新的正则表达式不会匹配 `/user/:id` 格式的路径
- 如果发生了，检查正则表达式是否正确

### Q3: 添加新的 `/user/` API 后报 404？
**解决方法**：
在 `vue.config.js` 的正则表达式中添加新的 API 名称：

```javascript
// 修改前
'^/user/(updateUserInfo|concernUser|unconcernUser)': { ... }

// 修改后（添加了 newApi）
'^/user/(updateUserInfo|concernUser|unconcernUser|newApi)': { ... }
```

### Q4: 更新头像成功但显示的还是旧头像？
**解决方法**：
1. 前端代码会在上传新头像后自动刷新页面：`location.reload()`
2. 如果没有刷新，手动刷新页面
3. 清除浏览器缓存
4. 检查后端的 `FileStorageService` 是否正确保存了新头像

## 技术细节

### 代理规则的优先级
Vue CLI 的 devServer.proxy 按照配置的**顺序**匹配规则：
1. 第一个匹配的规则会被应用
2. 后面的规则不会再检查

**当前顺序**：
```
1. /api
2. login|register|...
3. illustrations|mangas|...
4. /user/(updateUserInfo|concernUser|unconcernUser)  ← 新增
5. /files
```

### 为什么不用通配符？
❌ **不好的做法**：
```javascript
'^/user/.*': { ... }  // 会匹配所有 /user/ 开头的路径
```

**问题**：
- 会匹配前端路由 `/user/:id`
- 导致页面无法正常加载

✅ **好的做法**：
```javascript
'^/user/(updateUserInfo|concernUser|unconcernUser)': { ... }
```

**优点**：
- 只匹配特定的 API
- 不影响前端路由
- 易于维护和调试

## 修复时间
2025-11-02

## 状态
✅ **已完成修复**
- ✅ 添加了 `/user/` API 的代理配置
- ✅ 不影响前端路由 `/user/:id`
- ✅ 更新头像功能正常工作
- ✅ 关注/取消关注功能也会正常工作

## 测试清单
- [ ] 重启前端服务
- [ ] 访问个人主页
- [ ] 点击"编辑"按钮
- [ ] 上传新头像并保存
- [ ] 验证头像更新成功
- [ ] 测试关注/取消关注功能（可选）

