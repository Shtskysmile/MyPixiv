# 注册页面密保问题修改说明

## 修改时间
2025-11-01

## 修改内容

### ✅ 1. 允许用户自定义编写密保问题

**修改文件**: `mypixiv-vue/src/components/user/Register.vue`

#### 界面变化
- **之前**：用户只能看到预设的3个密保问题，只需填写答案
- **现在**：用户可以自己编写密保问题和答案

#### 具体改动

##### 数据结构更新
```javascript
// 之前
securityQuestions: [
  { question: '你最喜欢的颜色是什么？' },
  { question: '你的出生地是哪里？' },
  { question: '你母亲的姓名是什么？' }
],
securityAnswers: ['', '', '']

// 现在
securityQuestions: [
  { question: '', answer: '' },
  { question: '', answer: '' },
  { question: '', answer: '' }
]
```

##### 界面更新
每个密保问题现在包含两个输入框：
1. **问题输入框**：用户自己输入密保问题（例如：你最喜欢的颜色是什么？）
2. **答案输入框**：输入对应的答案

##### 表单验证
```javascript
// 验证密保问题和答案是否都已填写
for (let i = 0; i < 3; i++) {
  if (!this.securityQuestions[i].question.trim()) {
    this.error = `请填写密保问题 ${i + 1}`;
    return;
  }
  if (!this.securityQuestions[i].answer.trim()) {
    this.error = `请填写密保问题 ${i + 1} 的答案`;
    return;
  }
}
```

##### 样式美化
- 每个密保问题使用独立的卡片样式
- 鼠标悬停时有高亮效果
- 输入框带有渐变边框和焦点效果

### ✅ 2. 修复后端接口参数对齐

#### 参数名修正
- **错误的参数名**: `RSecurityIssues`
- **正确的参数名**: `SecurityIssues`

#### 数据格式对齐
根据后端 `R_SecurityIssue.java` 定义：
```java
public class R_SecurityIssue {
    private String description;  // 密保问题
    private String answer;       // 密保答案
}
```

前端发送数据格式：
```javascript
const securityIssuesJson = this.securityQuestions.map(q => ({
  description: q.question,  // 映射到 description
  answer: q.answer          // 映射到 answer
}));

// 使用Blob发送JSON数组
formData.append('SecurityIssues', new Blob([JSON.stringify(securityIssuesJson)], {
  type: 'application/json'
}));
```

### ✅ 3. 确认后端地址配置

**配置文件**: `mypixiv-vue/src/config/mock.config.js`

```javascript
apiBaseUrl: process.env.VUE_APP_API_BASE_URL || 'http://www.pcoi.top'
```

✅ 已确认：后端地址为 `http://www.pcoi.top`

## 后端接口定义

### 注册接口
**路径**: `POST /register`

**参数**:
- `username` (String, required) - 用户名
- `password` (String, required) - 密码
- `gender` (Integer, required) - 性别 (0=女, 1=男)
- `SecurityIssues` (List\<R_SecurityIssue\>, optional) - 密保问题列表
- `avatar` (MultipartFile, optional) - 头像文件

**SecurityIssues 格式**:
```json
[
  {
    "description": "你最喜欢的颜色是什么？",
    "answer": "蓝色"
  },
  {
    "description": "你的出生地是哪里？",
    "answer": "北京"
  },
  {
    "description": "你母亲的姓名是什么？",
    "answer": "张三"
  }
]
```

**响应**:
```json
{
  "code": 200,
  "message": "注册成功",
  "data": "注册成功"
}
```

## 使用说明

### 注册流程
1. 填写用户名、性别、密码
2. **编写3个密保问题和答案**（新增功能）
   - 问题可以自己设定，例如："你最喜欢的动漫角色是谁？"
   - 答案要记住，用于将来找回密码
3. 可选：上传头像
4. 点击"立即注册"

### 注意事项
1. **密保问题必须填写**：3个问题和答案都是必填项
2. **问题要有意义**：建议设置只有自己知道答案的问题
3. **答案要记住**：密保答案用于找回密码，请妥善保管
4. **答案区分大小写**：找回密码时需要完全匹配

## 测试建议

### 功能测试
- [x] 用户能够输入自定义的密保问题
- [x] 用户能够输入密保答案
- [x] 表单验证正确（问题和答案都必填）
- [x] 数据格式正确发送到后端
- [x] 注册成功后正确跳转

### 界面测试
- [x] 密保问题输入框显示正常
- [x] 答案输入框显示正常
- [x] 样式美观（卡片、渐变、悬停效果）
- [x] 响应式布局正常

### 接口测试
- [x] 参数名正确（SecurityIssues）
- [x] 数据格式正确（description, answer）
- [x] 后端能正确接收并存储
- [x] 密保答案能用于找回密码

## 相关文件

### 前端文件
- `mypixiv-vue/src/components/user/Register.vue` - 注册页面组件
- `mypixiv-vue/src/config/mock.config.js` - API配置

### 后端文件
- `mypixiv-springboot/src/main/java/org/example/PCOI/Controller/UserController.java` - 用户控制器
- `mypixiv-springboot/src/main/java/org/example/PCOI/ResponseDTO/R_SecurityIssue.java` - 密保问题DTO

## 示例

### 密保问题示例
1. **问题**: 你第一只宠物的名字是什么？  
   **答案**: 小白

2. **问题**: 你最喜欢的动漫角色是谁？  
   **答案**: 卡卡西

3. **问题**: 你高中最好的朋友叫什么？  
   **答案**: 李四

## 其他修复

### ✅ 修复找回密码页面的接口对齐

**文件**: `mypixiv-vue/src/components/user/ChangePwd.vue`

修改了验证密保问题的接口调用方式，从 URLSearchParams 改为 FormData + JSON Blob，与注册页面保持一致。

```javascript
// 之前：使用 URLSearchParams
const params = new URLSearchParams();
params.append('username', this.username);
this.questions.forEach((q, idx) => {
  params.append(`SecurityIssues[${idx}].question`, q);
  params.append(`SecurityIssues[${idx}].answer`, this.answers[idx]);
});

// 现在：使用 FormData + JSON Blob
const formData = new FormData();
formData.append('username', this.username);
const securityIssuesJson = this.questions.map((q, idx) => ({
  description: q,
  answer: this.answers[idx]
}));
formData.append('SecurityIssues', new Blob([JSON.stringify(securityIssuesJson)], {
  type: 'application/json'
}));
```

### ✅ 修正后端响应码检查

**文件**: `mypixiv-vue/src/components/user/Register.vue`

根据后端 `Result.java` 定义，成功响应码是 `0` 而不是 `200`。

```java
// 后端 Result.java
public static <E> Result<E> success(E data) {
    return new Result<>(0, "操作成功", data);  // code: 0 表示成功
}
```

前端修改：
```javascript
// 修改前
if (res.data && res.data.code === 200) { ... }

// 修改后
if (res.data && res.data.code === 0) { ... }
```

## 完成确认
- ✅ 用户可以自定义编写密保问题
- ✅ 前端接口参数名已修正为 SecurityIssues
- ✅ 数据格式与后端完全对齐（description, answer）
- ✅ 后端地址确认为 http://www.pcoi.top
- ✅ 界面美化完成（卡片样式、渐变效果）
- ✅ 表单验证完整（问题和答案都必填）
- ✅ 响应码修正为 code: 0
- ✅ 找回密码页面同步修复
- ✅ 无 lint 错误

