# 用户主页完整修复方案

## 修复的问题

### 问题 1：直接访问用户主页 URL 报 500 错误
**现象**：
```
GET http://localhost:8080/user/6c2762d4-b792-11f0-8af4-00ff77e93bfa 500 (Internal Server Error)
```

**原因**：`vue.config.js` 中的代理配置错误地将前端路由 `/user/*` 代理到了后端

**修复**：移除代理配置中的 `user` 路径

---

### 问题 2：头像加载 404 错误
**现象**：
```
GET http://localhost:8080/files/6c2762d4-b792-11f0-8af4-00ff77e93bfa/avatar/ 404 (Not Found)
```

**原因**：后端返回的是文件夹路径（以 `/` 结尾）而不是完整文件路径

**修复**：
1. 修改后端 `FileStorageService`，返回完整文件路径
2. 创建自动迁移工具修复旧数据
3. 前端兼容新旧两种格式

---

## 修改的文件

### 前端修改（mypixiv-vue）

#### 1. `vue.config.js`
**修改内容**：移除 `user` 路径的代理配置

```javascript
// 修改前（第 50 行）
'^/(illustrations|mangas|contribution|search|userInfo|contributionList|favouriteList|likedList|concernedList|user)': {

// 修改后
'^/(illustrations|mangas|contribution|search|userInfo|contributionList|favouriteList|likedList|concernedList|userCommentList)': {
```

**为什么修改**：
- `/user/*` 是前端路由，应该由 Vue Router 处理
- 代理到后端会导致 500 错误（后端没有 GET `/user/{id}` 接口）

---

#### 2. `src/components/user/User.vue`
**修改内容**：增强 `avatarSrc` computed 属性，兼容新旧两种头像路径格式

```javascript
computed: {
  avatarSrc() {
    if (!this.user.avatar) {
      return avatar; // 默认头像
    }
    // 如果是完整URL，直接返回
    if (this.user.avatar.startsWith('http')) {
      return this.user.avatar;
    }
    // 如果已经包含 /files/ 前缀，直接返回
    if (this.user.avatar.startsWith('/files/')) {
      return this.user.avatar;
    }
    // 否则拼接 /files/ 前缀
    return `/files/${this.user.avatar}`;
  },
}
```

**为什么修改**：
- 兼容新格式（完整文件路径）：`/files/userId/avatar/filename.jpg`
- 兼容旧格式（文件夹路径）：`/files/userId/avatar/`（虽然无法正常显示，但不会报错）

---

### 后端修改（mypixiv-springboot）

#### 3. `src/main/java/org/example/PCOI/Service/Support/FileStorageService.java`
**核心修复**：修改头像保存逻辑

**主要修改**：

1. **修改 `saveAvatar()` 方法**：返回完整文件路径
```java
public String saveAvatar(MultipartFile avatar, String userId) {
    if (avatar == null || avatar.isEmpty()) return null;
    String relFolder = Paths.get(sanitize(userId), avatarSubdir).toString();
    Path dir = Paths.get(uploadBaseDir, relFolder);
    String filename; // 新增：记录文件名
    try {
        Files.createDirectories(dir);
        clearDirectory(dir);
        filename = saveOneFileAndReturnName(avatar, dir); // 修改：获取文件名
    } catch (IOException e) {
        throw new RuntimeException("保存头像失败", e);
    }
    return buildFileUrl(relFolder, filename); // 修改：返回完整文件路径
}
```

2. **新增 `saveOneFileAndReturnName()` 方法**：保存文件并返回文件名
```java
private String saveOneFileAndReturnName(MultipartFile file, Path targetDir) throws IOException {
    String contentType = file.getContentType();
    if (contentType != null && !contentType.startsWith("image/")) {
        throw new IllegalArgumentException("仅支持图片类型文件");
    }
    String ext = getFileExtension(Objects.requireNonNullElse(file.getOriginalFilename(), ""));
    if (ext == null && contentType != null) {
        ext = mimeToExt(contentType);
    }
    String filename = UUID.randomUUID().toString().replace("-", "");
    filename = (ext == null || ext.isEmpty()) ? filename : filename + "." + ext;
    Path dest = targetDir.resolve(filename);
    file.transferTo(dest.toFile());
    return filename;
}
```

3. **新增 `buildFileUrl()` 方法**：构建完整文件 URL
```java
private String buildFileUrl(String relFolder, String filename) {
    String folderUrl = ensureTrailingSlash(uploadUrlPrefix) + 
                       (relFolder.startsWith("/") ? relFolder.substring(1) : relFolder);
    folderUrl = ensureTrailingSlash(folderUrl);
    return folderUrl + filename;
}
```

**影响**：
- ✅ 新注册的用户头像路径将使用完整格式
- ✅ 更新头像时也会使用完整格式
- ❌ 旧用户数据需要迁移（见下一个文件）

---

#### 4. `src/main/java/org/example/PCOI/Service/Support/AvatarPathMigration.java`（新增）
**功能**：自动迁移旧数据的头像路径

**特性**：
- 实现 `CommandLineRunner` 接口，应用启动时自动运行
- 检测所有用户的头像路径
- 将旧格式（文件夹路径，以 `/` 结尾）转换为新格式（完整文件路径）
- 自动更新数据库
- 提供详细的迁移日志
- 可通过配置项禁用

**核心逻辑**：
```java
@Override
public void run(String... args) throws Exception {
    if (!migrationEnabled) {
        System.out.println("⏭️  头像路径迁移已禁用");
        return;
    }

    System.out.println("🔄 开始检查头像路径迁移...");
    
    List<User> allUsers = userMapper.selectAllUsers();
    int updatedCount = 0;
    int errorCount = 0;

    for (User user : allUsers) {
        String avatar = user.getAvatar();
        
        // 检查是否是旧格式（以 / 结尾的文件夹路径）
        if (avatar != null && !avatar.isEmpty() && avatar.endsWith("/")) {
            try {
                String newAvatar = migrateAvatarPath(avatar);
                if (newAvatar != null && !newAvatar.equals(avatar)) {
                    user.setAvatar(newAvatar);
                    userMapper.updateUser(user);
                    updatedCount++;
                    System.out.println("✅ 已更新用户 " + user.getUsername() + " 的头像路径");
                }
            } catch (Exception e) {
                errorCount++;
                System.err.println("❌ 更新失败：" + e.getMessage());
            }
        }
    }

    System.out.println("✨ 头像路径迁移完成！");
    System.out.println("   - 总用户数：" + allUsers.size());
    System.out.println("   - 更新成功：" + updatedCount);
    System.out.println("   - 更新失败：" + errorCount);
}
```

**安全性**：
- ✅ 只更新以 `/` 结尾的路径（旧格式）
- ✅ 单个用户失败不影响其他用户
- ✅ 详细的错误日志
- ✅ 可以随时禁用

---

#### 5. `src/main/java/org/example/PCOI/Mapper/UserMapper.java`
**修改内容**：新增 `selectAllUsers()` 方法

```java
@Select("SELECT * FROM user")
List<User> selectAllUsers();
```

**为什么修改**：迁移工具需要获取所有用户数据

---

#### 6. `src/main/resources/application.properties`
**新增配置**：

```properties
# 头像路径迁移配置（将旧格式的文件夹路径转换为完整文件路径）
# 设置为 false 可以禁用自动迁移
pcoi.migration.avatar.enabled=true
```

**用途**：
- 控制是否启用自动迁移
- 迁移完成后可以设置为 `false` 提高启动速度

---

## 使用步骤

### 1. 重启前端服务
由于修改了 `vue.config.js`，必须重启前端服务：

```bash
cd mypixiv-vue
# 停止当前服务（Ctrl+C）
npm run serve
```

**验证**：
- ✅ 可以直接访问 `http://localhost:8080/user/xxx` 而不报错
- ✅ 前端路由正常工作

---

### 2. 重启后端服务
重启后端后，迁移工具会自动运行：

```bash
cd mypixiv-springboot
# 停止当前服务
# 重新启动 Spring Boot 应用
```

**控制台输出示例**：
```
🔄 开始检查头像路径迁移...
✅ 已更新用户 testuser 的头像路径：/files/6c2762d4-b792-11f0-8af4-00ff77e93bfa/avatar/d2e672ad3ab4497695e23be82adba65d.jpg
✅ 已更新用户 admin 的头像路径：/files/cb09759c-b793-11f0-8af4-00ff77e93bfa/avatar/a80142c22fb047e1ad5ebdbc763a8601.jpg
✨ 头像路径迁移完成！
   - 总用户数：5
   - 更新成功：5
   - 更新失败：0
```

---

### 3. 验证修复

#### 测试 1：直接访问用户主页
1. 在浏览器地址栏输入：`http://localhost:8080/user/6c2762d4-b792-11f0-8af4-00ff77e93bfa`
2. 验证：
   - ✅ 页面正常加载
   - ✅ 不再出现 500 错误
   - ✅ 用户信息正常显示

#### 测试 2：头像显示
1. 检查浏览器控制台：
   - ✅ 没有 404 错误
   - ✅ 头像图片加载成功
2. 检查头像 URL：
   - ✅ 格式正确：`http://localhost:8080/files/userId/avatar/filename.jpg`
   - ✅ 不以 `/` 结尾

#### 测试 3：导航栏
1. 点击导航栏用户头像
2. 验证：
   - ✅ 跳转到个人主页
   - ✅ 头像正常显示

#### 测试 4：新用户注册
1. 注册一个新用户并上传头像
2. 登录后访问个人主页
3. 验证：
   - ✅ 头像立即正常显示
   - ✅ 导航栏头像正常

---

### 4. 禁用迁移（可选）

数据迁移完成后，建议禁用迁移以提高启动速度：

**修改文件**：`mypixiv-springboot/src/main/resources/application.properties`

```properties
pcoi.migration.avatar.enabled=false
```

**注意**：禁用后不会影响已经迁移的数据和新用户注册

---

## 技术细节

### 路径格式对比

| 类型 | 旧格式（问题） | 新格式（修复后） |
|------|---------------|------------------|
| **后端返回** | `/files/userId/avatar/` | `/files/userId/avatar/d2e672ad3ab4497695e23be82adba65d.jpg` |
| **数据库存储** | `/files/userId/avatar/` | `/files/userId/avatar/d2e672ad3ab4497695e23be82adba65d.jpg` |
| **前端访问** | `http://localhost:8080/files/userId/avatar/` | `http://localhost:8080/files/userId/avatar/d2e672ad3ab4497695e23be82adba65d.jpg` |
| **访问结果** | ❌ 404 Not Found | ✅ 图片正常显示 |

### 代理配置说明

**前端路由 vs 后端 API**：

| 类型 | 路径 | 处理方式 |
|------|------|----------|
| 前端路由 | `/user/:id` | Vue Router 处理 |
| 后端 API | `/userInfo` | 代理到后端 8081 |
| 静态文件 | `/files/*` | 代理到后端 8081 |

**错误配置示例**（已修复）：
```javascript
// ❌ 错误：将前端路由代理到后端
'^/(illustrations|mangas|contribution|search|userInfo|user)': {
  target: 'http://localhost:8081'
}
```

**正确配置**：
```javascript
// ✅ 正确：只代理实际的后端 API
'^/(illustrations|mangas|contribution|search|userInfo|userCommentList)': {
  target: 'http://localhost:8081'
}
```

---

## 故障排查

### 问题：迁移后仍然显示默认头像
**可能原因**：
1. 浏览器缓存
2. localStorage 缓存

**解决方法**：
1. 清除浏览器缓存
2. 清除 localStorage：
   ```javascript
   localStorage.removeItem('userAvatar');
   ```
3. 重新登录

---

### 问题：迁移工具显示 "目录不存在" 或 "目录为空"
**可能原因**：
1. 文件已被删除
2. 文件系统路径配置错误

**解决方法**：
1. 检查 `application.properties` 中的 `pcoi.upload.base-dir` 配置
2. 确认文件系统中确实存在头像文件
3. 对于文件丢失的用户，提示重新上传头像

---

### 问题：新用户注册后头像仍然是文件夹路径
**可能原因**：后端代码未正确编译或重启

**解决方法**：
1. 确认后端代码修改已保存
2. 完全停止后端服务并重新编译运行
3. 检查控制台是否有编译错误

---

## 修复时间
2025-11-02

## 状态
✅ **已完成所有修复**
- ✅ 直接访问用户主页不再报 500 错误
- ✅ 头像加载不再报 404 错误
- ✅ 后端返回完整文件路径
- ✅ 前端兼容新旧格式
- ✅ 自动迁移工具已创建
- ✅ 新用户注册正常工作

## 测试清单
- [ ] 重启前端服务
- [ ] 重启后端服务（观察迁移日志）
- [ ] 直接访问用户主页 URL
- [ ] 检查头像是否正常显示
- [ ] 导航栏头像是否正常
- [ ] 注册新用户并上传头像
- [ ] 禁用迁移工具（可选）

