# 前端代理配置修复说明 - 解决 CORS 跨域问题

## 问题描述

前端尝试向后端发送注册请求时，出现 CORS 错误：

```
Access to XMLHttpRequest at 'http://www.pcoi.top/register' from origin 'http://localhost:8080' 
has been blocked by CORS policy: Response to preflight request doesn't pass access control check: 
No 'Access-Control-Allow-Origin' header is present on the requested resource.
```

## 问题原因

前端代码直接调用 `/register`、`/login` 等后端接口，但是 `vue.config.js` 中的代理配置只代理了 `/api` 开头的路径，导致这些请求直接发送到后端，触发了 CORS 跨域限制。

## 解决方案

### 1. 修改前端代理配置

**文件**: `mypixiv-vue/vue.config.js`

在原有的 `/api` 代理基础上，增加对用户认证接口和其他后端接口的代理：

```javascript
proxy: useMock ? {} : {
  // 代理 /api 开头的请求
  '/api': {
    target: process.env.VUE_APP_API_BASE_URL || 'http://www.pcoi.top',
    changeOrigin: true,
    pathRewrite: {
      '^/api': '/api'
    },
    logLevel: 'debug',
    onProxyReq: (proxyReq, req, res) => {
      console.log(`🔄 代理请求: ${req.method} ${req.url} -> ${proxyReq.path}`)
    },
    onProxyRes: (proxyRes, req, res) => {
      console.log(`✅ 代理响应: ${proxyRes.statusCode} ${req.url}`)
    },
    onError: (err, req, res) => {
      console.error('❌ 代理错误:', err.message)
      console.error('💡 提示: 请确保后端服务已启动')
    }
  },
  // 代理用户认证相关的请求
  '^/(login|register|mySecurityIssues|verifySecurityIssue|updatePassword)': {
    target: process.env.VUE_APP_API_BASE_URL || 'http://www.pcoi.top',
    changeOrigin: true,
    logLevel: 'debug',
    onProxyReq: (proxyReq, req, res) => {
      console.log(`🔄 代理请求: ${req.method} ${req.url} -> ${proxyReq.path}`)
    },
    onProxyRes: (proxyRes, req, res) => {
      console.log(`✅ 代理响应: ${proxyRes.statusCode} ${req.url}`)
    },
    onError: (err, req, res) => {
      console.error('❌ 代理错误:', err.message)
      console.error('💡 提示: 请确保后端服务已启动')
    }
  },
  // 代理其他不带 /api 前缀的后端接口
  '^/(illustrations|mangas|contribution|search|userInfo|contributionList|user)': {
    target: process.env.VUE_APP_API_BASE_URL || 'http://www.pcoi.top',
    changeOrigin: true,
    logLevel: 'debug',
    onProxyReq: (proxyReq, req, res) => {
      console.log(`🔄 代理请求: ${req.method} ${req.url} -> ${proxyReq.path}`)
    },
    onProxyRes: (proxyRes, req, res) => {
      console.log(`✅ 代理响应: ${proxyRes.statusCode} ${req.url}`)
    },
    onError: (err, req, res) => {
      console.error('❌ 代理错误:', err.message)
      console.error('💡 提示: 请确保后端服务已启动')
    }
  }
}
```

## 配置说明

### 1. 三组代理规则

#### 第一组：`/api` 开头的请求
- 匹配：`/api/xxx` 
- 转发到：`http://www.pcoi.top/api/xxx`
- 用途：API 接口

#### 第二组：用户认证相关
- 匹配：`/login`, `/register`, `/mySecurityIssues`, `/verifySecurityIssue`, `/updatePassword`
- 转发到：`http://www.pcoi.top/login`, `http://www.pcoi.top/register` 等
- 用途：用户登录、注册、密码找回

#### 第三组：其他后端接口
- 匹配：`/illustrations`, `/mangas`, `/contribution`, `/search`, `/userInfo`, `/contributionList`, `/user`
- 转发到：对应的后端接口
- 用途：作品、搜索、用户信息等

### 2. 关键配置项

- **`target`**: 后端服务器地址（`http://www.pcoi.top`）
- **`changeOrigin: true`**: ⭐ **关键配置** - 修改请求头中的 Origin，使后端认为请求来自同源
- **`logLevel: 'debug'`**: 输出详细的代理日志
- **钩子函数**: `onProxyReq`、`onProxyRes`、`onError` 用于监控和调试

### 3. changeOrigin 的作用

```
原始请求：
  Origin: http://localhost:8080
  Target: http://www.pcoi.top

经过代理后：
  Origin: http://www.pcoi.top (由代理服务器修改)
  Target: http://www.pcoi.top
```

这样后端就认为请求来自同源，不会触发 CORS 限制！

## 工作原理

```
┌─────────┐         ┌──────────────┐         ┌─────────┐
│  浏览器  │ ------> │ Vue Dev Server│ ------> │  后端   │
│localhost │         │  (代理服务器)  │         │www.pcoi │
│  :8080  │ <------ │   :8080       │ <------ │  .top   │
└─────────┘         └──────────────┘         └─────────┘
    同源请求             跨域代理                后端服务
   (无CORS问题)        (changeOrigin)        (看到同源请求)
```

### 流程说明

1. **浏览器发起请求**：`http://localhost:8080/register`
2. **Dev Server 拦截**：匹配到代理规则
3. **修改请求头**：`changeOrigin: true` 将 Origin 改为 `http://www.pcoi.top`
4. **转发到后端**：`http://www.pcoi.top/register`
5. **后端响应**：认为是同源请求，正常返回
6. **Dev Server 转发**：将响应返回给浏览器
7. **浏览器接收**：看到的是同源响应（`localhost:8080`），无 CORS 问题

## 测试验证

### 1. 重启前端开发服务器

```bash
# 停止当前运行的服务（Ctrl+C）
# 然后重新启动
npm run serve
```

### 2. 查看启动日志

```
========================================
🔌 Mock模式已关闭
📡 API请求将代理到: http://www.pcoi.top
💡 请确保后端服务已启动
🌐 前端服务运行在: http://localhost:8080
========================================
```

### 3. 测试注册功能

1. 打开浏览器：`http://localhost:8080`
2. 点击"注册"按钮
3. 填写注册信息
4. 点击"注册"提交

### 4. 查看代理日志

在终端中应该看到：

```
🔄 代理请求: POST /register -> /register
✅ 代理响应: 200 /register
```

### 5. 浏览器 Network 面板

- **Request URL**: `http://localhost:8080/register` （同源！）
- **Response Headers**: 正常返回，无 CORS 错误
- **Status**: 200 OK

## 注意事项

### 1. 只在开发环境生效

代理配置只在 `npm run serve` 开发模式下生效。

**生产环境**需要：
- 部署前端到与后端相同的域名下
- 或者在 Nginx 等反向代理服务器上配置代理
- 或者后端配置 CORS

### 2. Mock 模式下不启用

当 `VUE_APP_USE_MOCK=true` 时，代理配置不生效，所有请求被 Mock.js 拦截。

### 3. 正则表达式匹配

代理的 key 可以是：
- 字符串：`'/api'` - 精确匹配
- 正则：`'^/(login|register)'` - 模式匹配

### 4. 代理优先级

多个代理规则时，按照配置顺序匹配，**第一个匹配的规则生效**。

## 常见问题

### Q1: 修改后还是报 CORS 错误？

**解决方法**：
1. 确保已重启前端服务（`npm run serve`）
2. 清除浏览器缓存（Ctrl+Shift+Delete）
3. 检查 Mock 模式是否关闭（`VUE_APP_USE_MOCK=false`）

### Q2: 代理不生效？

**排查步骤**：
1. 检查终端日志，看是否输出代理信息
2. 检查请求路径是否匹配代理规则
3. 在 Network 面板查看 Request URL 是否是 `localhost:8080`

### Q3: 需要添加新的接口？

**操作方法**：

在第三组代理规则中添加路径：

```javascript
'^/(illustrations|mangas|contribution|search|userInfo|contributionList|user|新接口)': {
  // ...
}
```

或者单独添加一组规则：

```javascript
'^/新接口': {
  target: 'http://www.pcoi.top',
  changeOrigin: true
}
```

### Q4: 生产环境如何部署？

**推荐方案**：

#### 方案 1: 前后端部署在同一域名（推荐）

```
https://your-domain.com/          -> 前端静态资源
https://your-domain.com/api/      -> 后端 API
https://your-domain.com/register  -> 后端认证接口
```

使用 Nginx 反向代理：

```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    # 前端
    location / {
        root /path/to/frontend/dist;
        try_files $uri $uri/ /index.html;
    }
    
    # 后端 API
    location /api/ {
        proxy_pass http://localhost:9090/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    
    # 用户认证接口
    location ~ ^/(login|register|mySecurityIssues|verifySecurityIssue|updatePassword) {
        proxy_pass http://localhost:9090;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    
    # 其他后端接口
    location ~ ^/(illustrations|mangas|contribution|search|userInfo|contributionList|user) {
        proxy_pass http://localhost:9090;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

#### 方案 2: 后端配置 CORS（次选）

如果前后端必须部署在不同域名，需要后端配置 CORS。但这不在本次修复范围内。

## 其他修改

无需修改其他文件，前端代码保持不变：

- ✅ `Register.vue` - 继续使用 `/register`
- ✅ `Login.vue` - 继续使用 `/login`
- ✅ `ChangePwd.vue` - 继续使用 `/mySecurityIssues` 等
- ✅ 其他组件 - 无需修改

## 总结

通过在 `vue.config.js` 中配置代理规则，利用 Vue CLI 的 Dev Server 作为代理服务器，将前端请求转发到后端，并通过 `changeOrigin: true` 修改请求头，使后端认为请求来自同源，从而避免了 CORS 跨域问题。

这是一种**纯前端解决方案**，不需要修改后端配置，在开发环境下非常有效！

---

**修复日期**: 2025-11-01  
**修复方式**: 前端代理配置  
**影响范围**: 开发环境（`npm run serve`）  
**生产部署**: 需要 Nginx 等反向代理或后端 CORS 配置

