# 快速修复指南

## 问题
1. ✅ 直接访问 `/user/xxx` 报 500 错误（已修复）
2. ✅ 头像加载报 404 错误（已修复）
3. ✅ 更新头像报 404 错误（已修复）

## 快速修复步骤

### 1️⃣ 重启前端（必须）
由于修改了 `vue.config.js`，必须重启：

```bash
cd mypixiv-vue
# Ctrl+C 停止当前服务
npm run serve
```

### 2️⃣ 重启后端（必须）
自动迁移工具会在启动时运行：

```bash
cd mypixiv-springboot
# 重新启动 Spring Boot 应用
```

**观察控制台输出**：
```
🔄 开始检查头像路径迁移...
✅ 已更新用户 xxx 的头像路径
✨ 头像路径迁移完成！
   - 总用户数：5
   - 更新成功：5
   - 更新失败：0
```

### 3️⃣ 测试验证
1. 访问：`http://localhost:8080/user/6c2762d4-b792-11f0-8af4-00ff77e93bfa`
2. 检查：
   - ✅ 页面正常加载（不报 500）
   - ✅ 头像正常显示（不报 404）
3. 测试更新头像：
   - ✅ 点击"编辑"按钮
   - ✅ 选择新头像并保存
   - ✅ 不报 404 错误，更新成功

### 4️⃣ 禁用迁移（可选）
数据迁移完成后，编辑 `application.properties`：

```properties
pcoi.migration.avatar.enabled=false
```

然后重启后端。

---

## 修改的文件（无需手动操作，仅供参考）

### 前端
- ✅ `mypixiv-vue/vue.config.js` - 移除 `user` 路径代理 + 添加特定 API 代理
- ✅ `mypixiv-vue/src/components/user/User.vue` - 头像路径兼容处理

### 后端
- ✅ `FileStorageService.java` - 返回完整文件路径
- ✅ `AvatarPathMigration.java` - 自动迁移工具（新增）
- ✅ `UserMapper.java` - 新增 `selectAllUsers()` 方法
- ✅ `application.properties` - 新增迁移配置

---

## 故障排查

### Q: 迁移后仍然看不到头像？
**A**: 清除浏览器缓存或重新登录

### Q: 迁移工具报错 "目录不存在"？
**A**: 检查 `application.properties` 中的 `pcoi.upload.base-dir` 路径是否正确

### Q: 新用户注册后头像还是有问题？
**A**: 确保后端已完全重启并重新编译

---

## 详细文档
- `用户主页完整修复方案.md` - 完整的修复方案和技术说明
- `更新头像404问题修复说明.md` - 更新头像功能的修复说明

